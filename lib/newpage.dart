import 'package:flutter/material.dart';

class Newpage extends StatelessWidget {  @override
  Widget build(BuildContext context) {
  return Scaffold(
    appBar: AppBar(title: Text("")),
    body: Column(
      children: [
        Text('REGISTRATION SUCCESSFULL.....1',)
      ],
    )
  );
  }
}
